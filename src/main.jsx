import React from 'react' // librairie
import ReactDOM from 'react-dom/client' // librairie
import App from './App.jsx' // composant
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
